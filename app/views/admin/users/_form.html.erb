  <%= simple_form_for [:admin,@user], defaults: { input_html: { class: 'default_class' } } do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :admin,label:'Is an admin?' %>

  <%= f.button :submit %>
  <% end %>
